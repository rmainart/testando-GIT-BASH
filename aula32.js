// STRINGS

// Tipo primitivo
const mensagem = 'minha primeira mensagem';

// Tipo objeto
const outraMensagem = new String('bom dia');

// mensagem.starWith('') -> confirma a primeira palavra da frase
// mensagem.endWith('') -> confirma a ultima palavra da frase
// mensagem.incluedes('') -> confirma se a palavra dentro das aspas contem na frase
// mensagem.indexOf('') -> mostra qual indice comeca a palavra
// mensagem.replace('','') -> vai substituir a primeira palavra pela segunda.
// mensagem.trim() -> tira os espacos escedentes no inicio ou final.
// mensagem.split(' ')-> quebra a frase em varios pedaços separados pelo "espaço"
