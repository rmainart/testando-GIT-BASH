let nome = 'Rafael'; // string literal
let idade = 25; //number literal
let estaAprovado = true; // boolean true or else
let sobrenome; //Undefined (sem valor)
let corSelecionada = null // Resetar um valor

// todos acima são propriedades e vamos transformar em objeto.

let pessoal = {
    nome: 'Rafael',
    idade: 25,
    estaAprovado: true,
    sobrenome: 'de Souza da Silva',
};

console.log(pessoal);
